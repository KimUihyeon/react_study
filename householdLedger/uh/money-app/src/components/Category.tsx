import React from 'react';

interface CategoryProps {
    name : string,
}

export const Category = (props : CategoryProps) : React.FC => {

    return (
        <div>
            Category
        </div>
    )
}