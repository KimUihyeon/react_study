import React from 'react';

interface Props {
    name : string,
}

export const Form : React.FC<Props> = (props : Props) => {
    return (
        <div>
            Category Form
        </div>
    )
}